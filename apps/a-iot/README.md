# 성남시 시민안심공원 IoT 모니터링 시스템

> **실시간 3D 지도 기반 IoT 센서 통합 관제 플랫폼**

## 📋 개요

성남시 시민안심공원 IoT 모니터링 시스템은 성남시 관내 공원에 설치된 IoT 센서들을 통합 관리하고, 센서 데이터를 기반으로 발생하는 이벤트를 실시간으로 감지·대응할 수 있도록 설계된 스마트 관제 플랫폼입니다.

### 주요 특징

- **실시간 모니터링**: WebSocket(STOMP) 기반 실시간 센서 데이터 수신 및 이벤트 알림
- **3D 지도 시각화**: Cesium.js를 활용한 3차원 지형 및 건물 모델 위에 센서 위치 표시
- **통합 이벤트 관리**: 이벤트 발생부터 조치 완료까지 전 과정 추적 및 이력 관리
- **IoT 센서 관제**: 화재, 온도, 변위 등 다양한 센서 유형 지원 및 배터리 상태 모니터링
- **통계 및 분석**: 대시보드를 통한 직관적인 현황 파악 및 데이터 분석

---

## 🎯 서비스 소개

### 배경

도시 공원 내 시민 안전 확보를 위해 다양한 IoT 센서(화재감지, 온도, 변위 센서 등)가 설치되어 있습니다. 이러한 센서들로부터 수집되는 대량의 데이터를 효과적으로 모니터링하고, 이상 상황 발생 시 신속하게 대응하기 위한 통합 관제 시스템이 필요했습니다.

### 솔루션

본 시스템은 다음과 같은 기능을 제공합니다:

| 기능 | 설명 |
|------|------|
| **실시간 이벤트 감지** | 센서 임계값 초과 시 즉각적인 알림 발생 및 관리자 통보 |
| **이벤트 조치 관리** | 이벤트 상태 추적(대기 → 처리중 → 완료) 및 조치 이력 기록 |
| **지도 기반 가시화** | 3D 지도에서 센서 위치 및 상태를 직관적으로 확인 |
| **센서 등록/관리** | IoT 센서 정보 등록, 수정, Mobius 플랫폼 연동 |
| **통계 대시보드** | 공원별, 센서별, 이벤트별 현황을 차트와 통계로 제공 |
| **사용자 권한 관리** | 역할 기반 접근 제어(RBAC)를 통한 보안 관리 |

### 기대 효과

- ⚡ **신속한 대응**: 이벤트 발생 즉시 알림으로 골든타임 내 조치 가능
- 📍 **위치 기반 관제**: 3D 지도로 현장 상황 즉각 파악
- 📊 **데이터 기반 의사결정**: 축적된 이력 데이터를 통한 예방적 관리
- 🔐 **체계적 관리**: 권한별 접근 제어로 안전한 시스템 운영

---

## 🏗️ 시스템 아키텍처

### 기술 스택

```
┌─────────────────────────────────────────────────────────────┐
│                       Frontend                               │
├─────────────────────────────────────────────────────────────┤
│  React 19        │  TypeScript     │  Vite 7.0              │
│  Tailwind CSS v4 │  Zustand 5.0    │  SWR 2.3               │
│  Cesium.js 1.125 │  React Hook Form│  Recharts 3.3          │
│  Radix UI        │  shadcn/ui      │  STOMP WebSocket       │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                       Backend API                            │
├─────────────────────────────────────────────────────────────┤
│  REST API (Spring Boot)  │  WebSocket (STOMP Protocol)      │
│  JWT Authentication      │  Mobius IoT Platform 연동         │
└─────────────────────────────────────────────────────────────┘
```

### 프로젝트 구조

```
apps/a-iot/
├── src/
│   ├── components/
│   │   ├── elements/          # 검색바, 페이지네이션 등 기본 UI
│   │   ├── error/             # 에러 표시 컴포넌트
│   │   ├── layout/            # 레이아웃 (Admin, Dashboard)
│   │   ├── map/               # Cesium 지도 컴포넌트
│   │   └── ProtectedRoute.tsx # 인증 보호 라우트
│   │
│   ├── pages/
│   │   ├── main/
│   │   │   ├── dashboard/     # 대시보드 (전체보기/공원별)
│   │   │   ├── events/        # 이벤트 이력 관리
│   │   │   └── iot/           # IoT 센서 모니터링
│   │   └── management/
│   │       ├── devices/       # 센서 유형, CCTV 관리
│   │       ├── sites/         # 공원(사이트) 관리
│   │       └── users/         # 사용자/역할/권한 관리
│   │
│   ├── stores/
│   │   ├── authStore.ts       # 인증 상태 관리
│   │   ├── eventStore.ts      # 이벤트 데이터 관리
│   │   ├── notificationStore.ts # 실시간 알림
│   │   └── cesium/            # Cesium 전용 스토어
│   │       ├── viewerStore.ts   # 뷰어 초기화
│   │       ├── cameraStore.ts   # 카메라 제어
│   │       ├── markerStore.ts   # 마커 관리
│   │       ├── tilesetStore.ts  # 3D 타일셋
│   │       └── polygonStore.ts  # 폴리곤 관리
│   │
│   ├── services/
│   │   ├── hooks/             # API 호출 커스텀 훅 (25개+)
│   │   └── types/             # TypeScript 타입 정의
│   │
│   └── utils/                 # 유틸리티 함수
│
├── public/images/             # 아이콘 및 이미지 리소스
└── vite.config.ts             # 빌드 설정
```

---

## 📱 주요 기능

### 1. 대시보드

실시간 현황을 한눈에 파악할 수 있는 통합 대시보드를 제공합니다.

#### 전체보기 탭
- **통계 카드**: 공원 수, IoT 센서 수, CCTV 수, 관리자 수
- **이벤트 현황 차트**: 상태별(대기/처리중/완료) 파이 차트
- **3D 지도**: 전체 공원 위치를 마커로 표시

#### 공원별 보기 탭
- **공원 선택**: 드롭다운으로 특정 공원 선택
- **장치 현황**: 정상/주의/경고/위험/연결끊김 상태별 카운트
- **센서 목록**: 해당 공원의 모든 센서 실시간 상태
- **배터리 알림**: 배터리 20% 미만 센서 경고

### 2. 이벤트 관리

이벤트 발생부터 조치 완료까지 전 과정을 관리합니다.

- **이벤트 목록**: 필터링, 검색, 페이지네이션 지원
- **상세 정보**: 이벤트명, 등급, 상태, 발생시간
- **위치 확인**: 3D 지도에서 해당 센서 위치로 이동
- **조치 이력**: 타임라인 형태로 처리 과정 기록
- **상태 변경**: 대기(ACTIVE) → 처리중(IN_PROGRESS) → 완료(RESOLVED)

### 3. IoT 센서 모니터링

등록된 모든 IoT 센서의 상태를 실시간으로 모니터링합니다.

- **필터링**: 공원별, 장치 유형별 필터
- **검색**: 장치 ID, 이름, Object ID로 검색
- **상태 표시**: 색상 코드로 직관적인 상태 표현
- **Mobius 연동**: 외부 IoT 플랫폼과 동기화

#### 센서 상태 색상 코드

| 상태 | 색상 | 설명 |
|------|------|------|
| NORMAL | 🟢 초록 | 정상 작동 |
| CAUTION | 🟡 노랑 | 주의 필요 |
| WARNING | 🟠 주황 | 경고 상태 |
| DANGER | 🔴 빨강 | 위험 상태 |
| DISCONNECTED | ⚪ 회색 | 연결 끊김 |

### 4. 사이트(공원) 관리

공원 정보를 등록하고 관리합니다.

- **공원 등록/수정/삭제**: CRUD 기능
- **위치 설정**: Cesium 지도에서 폴리곤 직접 그리기
- **썸네일 관리**: 공원 대표 이미지 설정
- **가상 순찰**: 경로 기반 순찰 기능

### 5. 센서 유형 관리

IoT 센서 유형별 이벤트 조건을 설정합니다.

- **장치 유형 목록**: 화재, 온도, 변위 등
- **이벤트 조건 설정**: 임계값, 비교 연산자, 이벤트 등급 설정
- **실시간 검증**: 조건 설정 시 유효성 검사

### 6. 사용자/권한 관리

역할 기반 접근 제어(RBAC)를 지원합니다.

- **사용자 관리**: 생성, 수정, 삭제, 비밀번호 초기화
- **역할 관리**: 역할 정의 및 권한 할당
- **권한 관리**: 리소스별 세분화된 권한 설정

---

## 🗺️ 3D 지도 시스템

### Cesium.js 기반 지도 기능

본 시스템은 Cesium.js를 활용하여 고품질 3D 지도 서비스를 제공합니다.

#### 지도 레이어

| 레이어 | 설명 |
|--------|------|
| **기본 지도** | Google Maps 이미지 레이어 |
| **위성 지도** | Google Satellite 이미지 |
| **지형** | Cesium World Terrain |
| **3D 건물** | Google Photorealistic 3D Tiles |
| **성남시 타일셋** | 로컬 3D 모델 (nginx 서버) |

#### 마커 시스템

- **공원 마커**: 원형 썸네일 이미지
- **센서 마커**: 상태별 색상 코딩된 SVG 아이콘
- **애니메이션**: 위험/경고 상태 시 깜빡임 효과
- **호버 효과**: 마커 확대 및 라벨 표시

#### 성능 최적화

```typescript
// LOD (Level of Detail) 설정
maximumScreenSpaceError: 32        // 품질/성능 균형
skipLevelOfDetail: true            // 빠른 로딩

// 렌더링 최적화
requestRenderMode: true            // 필요 시에만 렌더링
depthTestAgainstTerrain: true      // 정확한 깊이 테스트

// 카메라 기반 타일셋 로딩
autoHideDistance: 2000m            // 2km 이상 거리에서 타일셋 숨김
```

---

## 🔄 실시간 통신

### WebSocket (STOMP Protocol)

실시간 데이터 통신을 위해 STOMP 프로토콜을 사용합니다.

```typescript
// 연결 설정
endpoint: /api/stomp/platform
heartbeat: 5초 (송신/수신)
reconnect: 5초 후 자동 재연결

// 구독 채널
/user/queue/sensor-alarm        // 새 이벤트 알림
/user/queue/connection-error    // 장치 연결 끊김
/user/queue/change-event-status // 이벤트 상태 변경
```

### 데이터 흐름

```
IoT 센서 → Mobius Platform → Backend API → WebSocket → Frontend
                                    ↓
                              REST API (SWR)
```

---

## ⚙️ 환경 설정

### 환경 변수 (.env)

```bash
# Cesium Ion 설정
VITE_CESIUM_ION_ACCESS_TOKEN=your_cesium_token
VITE_CESIUM_GOOGLE_MAP_ASSET_ID=3830182
VITE_CESIUM_TERRAIN_ASSET_ID=3825983
VITE_CESIUM_GOOGLE_3D_TILES_ASSET_ID=2275207

# 공원별 3D 타일셋
VITE_CESIUM_CENTER_PARK_ASSET_ID=4004889
VITE_CESIUM_YD_PARK_ASSET_ID=4005051

# 높이 오프셋 (미터)
VITE_CESIUM_CENTER_PARK_HEIGHT_OFFSET=0
VITE_CESIUM_YD_PARK_HEIGHT_OFFSET=0
VITE_CESIUM_SEONGNAM_HEIGHT_OFFSET=20

# 로컬 타일셋 서버
VITE_LOCAL_TILESET_BASE_URL=http://192.168.0.150

# API 설정
VITE_API_URL=/api
```

---

## 🚀 실행 방법

### 개발 환경

```bash
# 의존성 설치
pnpm install

# 개발 서버 실행 (포트 4000)
pnpm dev

# 타입 체크
pnpm type-check

# 린트
pnpm lint
```

### 빌드

```bash
# 개발 빌드
pnpm build:develop

# 스테이징 빌드
pnpm build:staging

# 프로덕션 빌드
pnpm build:production
```

### 요구 사항

- Node.js >= 22.0.0
- pnpm >= 10.0.0

---

## 📦 주요 의존성

| 패키지 | 버전 | 용도 |
|--------|------|------|
| react | 19.0.0 | UI 프레임워크 |
| cesium | 1.125.0 | 3D 지도 |
| zustand | 5.0.8 | 상태 관리 |
| swr | 2.3.6 | 데이터 페칭/캐싱 |
| @stomp/stompjs | 7.2.1 | WebSocket 통신 |
| react-hook-form | 7.65.0 | 폼 관리 |
| zod | 4.1.11 | 스키마 검증 |
| recharts | 3.3.0 | 차트 |
| tailwindcss | 4.0.0 | CSS 스타일링 |

---

## 🔐 보안

- **JWT 인증**: 토큰 기반 사용자 인증
- **RBAC**: 역할 기반 접근 제어
- **API 타임아웃**: 30초
- **CORS**: Vite 프록시를 통한 처리
- **권한 검사**: ProtectedRoute 컴포넌트로 페이지별 접근 제어

---

## 📄 API 문서

- **API 문서**: http://dev.pluxity.com/api/api-docs
- **Swagger UI**: http://dev.pluxity.com/api/swagger-ui/index.html

---

## 🏢 라이선스

© 2025 PLUXITY. All rights reserved.

---

## 📞 문의

프로젝트 관련 문의사항은 GitHub Issues를 통해 등록해 주세요.

**Repository**: [pluxity/plug-platform-atlas](https://github.com/pluxity/plug-platform-atlas)
